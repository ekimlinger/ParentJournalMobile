# Uncomment this line to define a global platform for your project
platform :ios, '9.0'

target 'Parent Journal' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Parent Journal
  pod 'Alamofire', '~> 3.4'

  target 'Parent JournalTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Parent JournalUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
